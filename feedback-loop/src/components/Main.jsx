import React, { useContext } from 'react';
import { DataContext } from './hidden/DataContext';

function Main(props) {
    return (
        <div>
            <h1>main component</h1>
        </div>
    );
}

export default Main;